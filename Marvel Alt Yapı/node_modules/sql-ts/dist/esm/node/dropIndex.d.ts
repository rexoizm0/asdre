import { Node } from '.';
import { Column } from '../column';
import { Table } from '../table';
export declare class DropIndexNode extends Node {
    table: Table<unknown>;
    options: {
        indexName: string;
        ifExists?: boolean;
    };
    constructor(table: Table<unknown>, indexName: string | string[] | Column<unknown>[]);
    ifExists(): DropIndexNode;
}
