import { Node, ParameterNode } from '.';
export class ModifierNode extends Node {
    constructor(query, type, count) {
        super(type);
        this.query = query;
        this.count = ParameterNode.getNodeOrParameterNode(count);
    }
}
//# sourceMappingURL=modifier.js.map