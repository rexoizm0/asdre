import { Node } from '.';
import { INodeable } from '../nodeable';
import { Table } from '../table';
export declare class WhereNode extends Node {
    table: Table<unknown>;
    constructor(table: Table<unknown>);
    add(node: Node[] | Node | INodeable | object | string): this;
    or(other: Node | object | string): void;
    and(other: Node[] | Node | object | string): void;
}
