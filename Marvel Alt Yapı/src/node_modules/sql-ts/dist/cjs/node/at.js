'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
exports.AtNode = void 0;
const tslib_1 = require("tslib");
const extend_1 = tslib_1.__importDefault(require("lodash/extend"));
const _1 = require(".");
let valueExpressionMixed = false;
class AtNode extends _1.Node {
    constructor(value, index) {
        super('AT');
        this.value = value;
        this.index = index;
        // Delay mixin to runtime, when all nodes have been defined, and
        // mixin only once. ValueExpressionMixin has circular dependencies.
        if (!valueExpressionMixed) {
            valueExpressionMixed = true;
            extend_1.default(AtNode.prototype, _1.valueExpressionMixin());
        }
    }
}
exports.AtNode = AtNode;
extend_1.default(AtNode.prototype, _1.AliasNode.AliasMixin);
//# sourceMappingURL=at.js.map