import { ColumnNode, Node } from '.';
import { Column } from '../column';
import { INodeable } from '../nodeable';
export declare class ReplaceNode extends Node {
    columns: ColumnNode[];
    names: string[];
    valueSets: {
        [key: string]: ColumnNode;
    }[];
    constructor();
    add(nodes: Column<unknown>[] | Node | INodeable | string): this;
    getParameters(): Node[][];
}
