import { Node, Query } from '.';
export declare class ModifierNode extends Node {
    query: Query<unknown>;
    count: Node;
    constructor(query: Query<unknown>, type: 'LIMIT' | 'OFFSET', count: unknown);
}
