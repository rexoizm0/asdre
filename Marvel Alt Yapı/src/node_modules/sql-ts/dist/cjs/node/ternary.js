'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
exports.TernaryNode = void 0;
const tslib_1 = require("tslib");
const extend_1 = tslib_1.__importDefault(require("lodash/extend"));
const _1 = require(".");
let valueExpressionMixed = false;
class TernaryNode extends _1.Node {
    constructor(config) {
        super('TERNARY');
        this.left = config.left;
        this.middle = config.middle;
        this.operator = config.operator;
        this.right = config.right;
        this.separator = config.separator;
        // Delay mixin to runtime, when all nodes have been defined, and
        // mixin only once. ValueExpressionMixin has circular dependencies.
        if (!valueExpressionMixed) {
            valueExpressionMixed = true;
            extend_1.default(TernaryNode.prototype, _1.valueExpressionMixin());
        }
    }
}
exports.TernaryNode = TernaryNode;
extend_1.default(TernaryNode.prototype, _1.AliasNode.AliasMixin);
//# sourceMappingURL=ternary.js.map