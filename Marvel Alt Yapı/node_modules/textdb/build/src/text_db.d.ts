import MemoryDB from './memory_db';
import { Record } from './memory_db';
export interface TextDBConfig {
    path?: string;
    activeLog?: boolean;
}
export declare class TextDB extends MemoryDB {
    private schema;
    private path;
    private activeLog;
    constructor(options: TextDBConfig);
    private loadTables(tables);
    private loadRecords(tables);
    private parseDataLine(line, fieldDescrs);
    insert(name: string, data: Record): string | number;
    update(tableName: string, id: string | number, changes: {
        [key: string]: any;
    }): void;
    delete(table: string, id: string | number): void;
    private log(txt);
}
