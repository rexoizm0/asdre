import { Query } from '../node';
import { Table } from '../table';
export declare abstract class Dialect {
    config: any;
    constructor(config: any);
    abstract getQuery(queryNode: Query<unknown> | Table<unknown>): {
        text: string;
        values: string[];
    };
    abstract getString(queryNode: Query<unknown>): string;
}
